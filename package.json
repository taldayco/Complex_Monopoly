{
  "name": "monopoly-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "wrangler types --check && vite build",
    "preview": "npm run build && wrangler dev",
    "start": "node server.js",
    "test": "node --test 'src/lib/engine/__tests__/*.test.js'",
    "gen": "wrangler types",
    "check": "wrangler types --check",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^7.2.8",
    "@sveltejs/kit": "^2.5.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "svelte": "^5.0.0",
    "vite": "^5.4.0",
    "wrangler": "^4.81.0"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
